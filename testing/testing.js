const dwinda = ["testing","halo"]


function dwitest(abc) {
    dwinda.forEach(element => {
        for (let index = 0; index < array.length; index++) {
            const element = array[index];
            
        }
        resizeBy([i])
    });
}